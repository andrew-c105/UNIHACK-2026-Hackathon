# Backend (Railway / Render / local)
SUPABASE_URL=
SUPABASE_KEY=
TRACKSYNC_DATA_DIR=./data
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Frontend (Vercel) — set to your deployed backend URL, no trailing slash
VITE_API_URL=http://localhost:8000
